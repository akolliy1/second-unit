steps:
  - name: gcr.io/cloud-builders/docker
    args: [build, -f, deploy/Dockerfile.seeder, -t, '${_IMAGE}:latest', .]
images:
  - '${_IMAGE}:latest'
options:
  logging: CLOUD_LOGGING_ONLY
timeout: 900s
